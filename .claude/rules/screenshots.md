# Screenshot & Image Cleanup

- Screenshot and design reference images (*.png, *.jpg, *.jpeg) in the project root are gitignored
- Delete any leftover screenshot files from the project root when you notice them
- Only images inside `public/` belong in the repo
